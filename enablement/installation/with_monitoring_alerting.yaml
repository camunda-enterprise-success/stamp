orchestration:
  env:
    - name: ZEEBE_BROKER_EXECUTION_METRICS_EXPORTER_ENABLED
      value: "true"

prometheusServiceMonitor:
  enabled: true

elasticsearch:
  metrics:
    extraArgs:
      - --es.indices_settings
      - --es.ilm
    enabled: true
    serviceMonitor:
      enabled: true